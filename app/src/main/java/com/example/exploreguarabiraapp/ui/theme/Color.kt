package com.example.exploreguarabiraapp.ui.theme

import androidx.compose.ui.graphics.Color

/* =====================================================
   Brand Colors
   ===================================================== */

// Light
val PrimaryBlue = Color(0xFF2563EB)      // Blue 600
val SecondaryIndigo = Color(0xFF4F46E5)  // Indigo 600
val AccentAmber = Color(0xFFFFC107)     // Amber 500

// Dark
val PrimaryBlueDark = Color(0xFF93C5FD)      // Blue 300
val SecondaryIndigoDark = Color(0xFFA5B4FC)  // Indigo 300
val AccentAmberDark = Color(0xFFFFD54F)      // Amber 300

/* =====================================================
   Backgrounds
   ===================================================== */

// Light
val BackgroundLight = Color(0xFFF8FAFC)
val SurfaceLight = Color(0xFFFFFFFF)

// Dark
val BackgroundDark = Color(0xFF0F172A) // Slate 900
val SurfaceDark = Color(0xFF020617)    // Almost black

/* =====================================================
   Gradiente do Dashboard
   ===================================================== */

// Light
val DashboardGradientTopLight = Color(0xFFE3F2FD)
val DashboardGradientBottomLight = Color(0xFFE8EAF6)

// Dark
val DashboardGradientTopDark = Color(0xFF1E293B)
val DashboardGradientBottomDark = Color(0xFF020617)

/* =====================================================
   Avaliação
   ===================================================== */

val RatingStarActiveLight = AccentAmber
val RatingStarActiveDark = AccentAmberDark

val RatingStarInactiveLight = Color(0xFFD1D5DB)
val RatingStarInactiveDark = Color(0xFF475569)

/* =====================================================
   Cores da Categoria
   ===================================================== */

val CafeColor = Color(0xFFF59E0B)
val RestauranteColor = Color(0xFFEF4444)
val TapiocariaColor = Color(0xFFEAB308)
val FitnessColor = Color(0xFF3B82F6)
val ShoppingColor = Color(0xFFA855F7)
